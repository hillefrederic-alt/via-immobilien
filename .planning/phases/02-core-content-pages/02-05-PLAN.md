---
phase: 02-core-content-pages
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/Footer.astro
autonomous: true

must_haves:
  truths:
    - "Footer displays working Instagram link"
    - "Instagram link opens in new tab"
    - "Footer social links section is visually styled"
  artifacts:
    - path: "src/components/Footer.astro"
      provides: "Footer with social media links"
      contains: "instagram"
  key_links:
    - from: "src/components/Footer.astro"
      to: "instagram.com"
      via: "anchor href"
      pattern: "href=.*instagram"
---

<objective>
Update the Footer component to include working social media links (Instagram) with proper styling.

Purpose: Social proof and brand presence - visitors expect social media links and may want to follow Via Immobilien on Instagram.

Output: Footer with styled, clickable Instagram link that opens in a new tab.
</objective>

<execution_context>
@/Users/frederichille/.claude/get-shit-done/workflows/execute-plan.md
@/Users/frederichille/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Existing footer to modify
@src/components/Footer.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add social media links to Footer</name>
  <files>src/components/Footer.astro</files>
  <action>
Read the existing Footer.astro and update it to include social media links section.

**Requirements:**

1. **Social links section:**
   - Add a "Folgen Sie mir" or "Social Media" section
   - Include Instagram link (placeholder URL: https://instagram.com/via_immobilien or similar)
   - Use target="_blank" and rel="noopener noreferrer" for external links

2. **Instagram icon:**
   - Use an SVG icon for Instagram (inline SVG preferred for styling control)
   - Alternative: Use text "Instagram" with an icon prefix
   - Icon should be ~24px size

3. **Styling:**
   - Social links should have hover states (color change, scale, or opacity)
   - Use existing design tokens (--color-gold for hover, --color-cream for default)
   - Ensure good contrast against footer background
   - Add subtle transition for hover effect

4. **Layout:**
   - Social links can be in their own row or integrated with existing footer content
   - Ensure mobile-friendly tap targets (min 44px)

**SVG for Instagram icon (if needed):**
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
  <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
  <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
</svg>
```

**Keep existing footer functionality intact** - legal links, copyright, etc.
  </action>
  <verify>
Run `npm run dev` and scroll to footer:
- Instagram link is visible
- Clicking opens instagram.com in new tab
- Hover state works (visual feedback)
- Icon displays correctly
  </verify>
  <done>
Footer displays:
- Instagram link with icon
- Link opens in new tab (target="_blank")
- Hover state provides visual feedback
- Mobile-friendly tap target size
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Footer shows Instagram link with icon
3. Clicking Instagram opens in new tab
4. Hover effect works
5. Footer still shows legal links (Impressum, Datenschutz)
6. `npm run build` completes without errors
</verification>

<success_criteria>
- Instagram link visible in footer
- Link has proper external link attributes (target, rel)
- Icon displays at appropriate size
- Hover state provides clear feedback
- Accessible (keyboard focusable, proper focus styles)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-content-pages/02-05-SUMMARY.md`
</output>
